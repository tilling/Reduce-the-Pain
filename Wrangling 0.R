#Wrangling the data sets

#Read them in
read_idfile <- function() {}

read_visit <- function() {}

idfile <- read_idfile("ID.csv")
visits = vector('list', length=4)
for (i in 1:4) {
  visits[[i]] <- read.csv("")
}

#Join

#Create ages at each visit time


#Apply lm to each group